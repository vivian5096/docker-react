language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t vivianwong/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vivianwong/docker-react npm run test -- --coverage
